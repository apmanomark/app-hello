#machine:

  #timezone:
    #Europe/London # Set the timezone

  # Version of ruby to use
  #ruby:
    #version:
      #1.9.3

  # Override /etc/hosts
  #hosts:
    #circlehost: 127.0.0.1
    #dev.apmano.com: 127.0.0.1

  # Add some environment variables
  #environment:
    #TF_API_TOKEN: ba0eece2d940f181bc515d98bfe01bd3_MTkwMjMwMDIwMTQtMDYtMDkgMTE6MTA6MDUuOTA1ODU3
    #TF_TEAM_TOKEN: 4a6014a1548c3d07d2097abbdfb5d36b_MzkxMzc3MjAxNC0wNi0wOSAxMzowMjo0OS4zMDYxMTU

#general:
  #build_dir: build


## Customize dependencies

dependencies:
  pre:
    # Install Sencha CMD
    - chmod +x sencha-cmd.sh 
    - sh ./sencha-cmd.sh # sencha cmd (for easy phonegap build intergration) # http://www.sencha.com/forum/showthread.php?278435-Install-over-command-line
    
    # Install Phonegap CLI
    - npm install -g phonegap # phonehap cli
